# ts-client

This folder contains the static browser client assets served by the TypeScript server. It includes HTML, loader/config scripts, input handling, and client-side display code used by the playable web app.

The server points at this folder through `ts-server/config.ts`. Most gameplay, networking, entities, and API logic live in `ts-server/`, not here.
