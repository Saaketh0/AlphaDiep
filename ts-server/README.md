# ts-server

This folder is the Node/TypeScript game server package. It contains the HTTP/WebSocket entry point, game modes, entities, physics helpers, protocol coder, configuration, Dockerfile, and npm scripts.

Install and run from here:

```bash
npm install
npm run server
```

It serves `../ts-client/` for the browser client and remains the reference implementation for C++ parity.
