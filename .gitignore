lib/
node_modules/
prev/
cpp-build/*
!cpp-build/README.md
.DS_Store
ext/
.vscode/
package/
ts-client/public/
replit.nix
.grepai/
.omx/

# TypeScript server package outputs
ts-server/lib/
ts-server/node_modules/

# --- Python / local dev ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- RL training artifacts (local SSD; see rl/runtime/league_initialization/paths.py) ---
# League safetensors (~GB), Redis AOF/RDB, RLlib Tune checkpoints, W&B runs,
# and env-runner probe cache (training_data/env_runner_capacity.json).
training_data/*
!training_data/README.md

# Legacy Redis bind mount (pre training_data/redis-server/)
redis_data/

# Observability eval outputs (legacy path; primary runs live under training_data/W&B/)
rl/observability/runs/**
!rl/observability/runs/.gitkeep
!rl/observability/runs/benchmarks.md

# W&B scratch dirs if created inside the repo
wandb/
.wandb/
